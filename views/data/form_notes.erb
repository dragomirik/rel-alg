<div class="guide-content">
    <p class="text-muted">
        Input attributes & data in CSV format, with one line per attribute and one line per data row.
    </p>

    <div class="mb-4">
        <h6 class="guide-subtitle">Attribute Format</h6>
        <p class="mb-2">Each attribute should be defined as:</p>
        <div class="code-block">
            <code>attribute_name,attribute_type</code>
        </div>
        <p class="mt-2 mb-1">Available types:</p>
        <ul class="type-list">
            <li><code>numeric</code> - for numbers</li>
            <li><code>string</code> - for text</li>
            <li><code>date</code> - for dates</li>
        </ul>
    </div>

    <div class="example-section">
        <h6 class="guide-subtitle">Example</h6>
        <p class="mb-2">For a <code>Users</code> relation with three attributes:</p>
        
        <div class="example-block mb-4">
            <div class="example-label">Attributes:</div>
            <pre class="example-code">id,numeric
email,string
name,string</pre>
        </div>

        <div class="example-block">
            <div class="example-label">Data rows:</div>
            <pre class="example-code">1,john@example.com,John Smith
2,jane@example.com,Jane Doe</pre>
        </div>
    </div>
</div>
